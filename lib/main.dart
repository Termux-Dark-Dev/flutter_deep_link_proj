import 'package:flutter/material.dart';
import 'package:media_social/app.dart';
// import 'package:media_social/utils/web_config.dart';

void main() {
  // For disabling the hash based routing in web : Flutter by default uses hash based routing, comment the below code when building for android otherwise it will not build
  // configureWeb();

  runApp(const MyApp());
}
